using UnityEngine;
using System;
namespace Support
{
    public enum Difficulty
    {
        Easy,
        Medium,
        Hard
    }

    public enum PlayState
    {
        Begin,
        Paused,
        Playing, 
        Dead
    }
}

